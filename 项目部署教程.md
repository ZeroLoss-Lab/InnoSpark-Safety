我还没写完，最好明天等我，不然有坑

建议点code观看，就当txt了。没想到md换行不是直接换的

进程管理: 建议使用tmux或类似工具分别运行四个核心组件，便于监控和重启。

推荐启动顺序：

#注意：我默认使用了tmux以及conda。我的显卡配置是CUDA 550-server，CUDA_HOME=/usr/local/cuda-12.4，版本没差很多应该影响不大

前/后拦截:
tmux new -s intercept
（tmux内）
cd api_safe/safe_api
conda create -n safe python=3.10 -y
conda activate safe
pip install -r requirements.txt
source setup_env.sh
python start_services.py
看到“前拦截服务启动成功”“后拦截服务启动成功”的提示，先按Ctrl+B，然后按D退出TMUX

RAG: 
tmux new -s rag
（tmux内）
cd rag_safe
conda create -n rag python=3.10 -y     # 若已创建可省略
conda activate rag
pip install llama-index
pip install llama-index-vector-stores-chroma
pip install llama-index-embeddings-huggingface
pip install llama-index-retrievers-bm25
pip install fastapi
pip install uvicorn
pip install chromadb
pip install nest-asyncio
pip install pystemmer
python server.py #启动服务脚本
初次创建会卡很久以构筑索引，建议保留当前tmux会话并在终端Ctrl+B然后D先做别的
正常输出大约如下：
  @app.on_event("startup")
INFO:     Started server process [8480]
INFO:     Waiting for application startup.
正在初始化检索器...
正在加载文档: rag_safe/data/sample_corpus.jsonl
成功加载示例文档
正在分割文档...
正在创建文档存储...
正在创建向量存储...
正在创建向量索引...
Generating embeddings: 100%|███████████████████████████████████████████████████████| 
成功信息：检索器初始化成功！先按Ctrl+B，然后按D退出TMUX

vLLM（即72B大模型）: 
tmux new -s vllm72b
cd 我不知道/0830_RL
conda create -n vllm python=3.10 -y
conda activate vllm
pip install "torch==2.7.1" "torchvision==0.22.1" "torchaudio==2.7.1" --index-url https://download.pytorch.org/whl/cu121
pip install vllm==0.4.2.post1
#下一段是四卡部署推荐配置，需要一直复制到port 9001
python -m vllm.entrypoints.openai.api_server \
  --model 0830_RL \
  --served-model-name InnoSpark \
  --tensor-parallel-size 4 \
  --gpu-memory-utilization 0.88 \
  --max-model-len 4096 \
  --max-num-seqs 64 \
  --port 9001
启动可能会卡一会，此时可见GPU利用率100%，启动后GPU利用率变0%

主API: 
#千万别开生产环境那个env，可能有不安全的“向外网暴露所有API接口”的配置
#开INFO级别日志，启动后会提示有不明IP进行无效访问，正常现象，可无视
tmux new -s mainapi
cd api_v2
./start_main_api.sh
启动后可见输出：
2025-10-12 11:35:16,235 - logger_config - INFO - 统一拦截架构已启用
2025-10-12 11:35:16,235 - logger_config - INFO - 拦截模式: 远程API
2025-10-12 11:35:16,235 - logger_config - INFO - 前拦截: 启用
2025-10-12 11:35:16,235 - logger_config - INFO - 后拦截: 启用
2025-10-12 11:35:16,235 - logger_config - INFO - RAG系统: 启用
大模型需要进行请求，看输出确认是否启动
